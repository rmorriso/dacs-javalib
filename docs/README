make-xxx creates the document xxx by calling make with the
values of BOOK_DIR, BOOK_NAME and INSTALL_DIR set appropriately.

Note also that the output files generated by make (xxx.fo, xxx.html
xxx.pdf and xxx.ps) should not be placed under version control, nor
should the directory xxx/html-chunks.
